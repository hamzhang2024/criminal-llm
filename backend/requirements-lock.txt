# Criminal LLM 后端依赖精确锁文件
#
# 用途：CI/打包可复现。安装此文件可得到与已验证环境完全一致的依赖版本。
# 生成方式：基于已验证可用环境 pip freeze + 后端依赖子树筛选
# 更新方式：升级依赖后运行 `python3 -m pip freeze | grep -iE '<包名>'` 重新生成
#
# 直接依赖见 requirements.txt（含版本下限，允许补丁更新）
# 本文件为精确版本（==），仅用于打包/CI 复现

# ---- Web 框架 ----
fastapi==0.129.0
starlette==0.52.1
sse-starlette==3.4.4  # SSE 实时推送（进度条/状态，缺失则所有 SSE 端点 500）
uvicorn==0.40.0
python-multipart==0.0.22

# ---- 数据校验 ----
pydantic==2.13.0
pydantic-core==2.46.0
annotated-types==0.7.0
annotated-doc==0.0.4
typing-extensions==4.15.0
typing-inspection==0.4.2

# ---- PDF 处理 ----
PyMuPDF==1.24.14
pdf2image==1.17.0
pillow==12.2.0

# ---- HTTP 客户端 ----
httpx==0.28.1
httpcore==1.0.9
anyio==4.12.1
sniffio==1.3.1
h11==0.16.0
certifi==2026.4.22
idna==3.11

requests==2.32.5
charset-normalizer==3.4.4
urllib3==2.6.3

aiohttp==3.13.3
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
attrs==25.4.0
frozenlist==1.8.0
multidict==6.7.1
propcache==0.4.1
yarl==1.22.0

# ---- 工具 ----
python-dotenv==1.2.1
click==8.4.1
six==1.17.0

# ---- C 扩展支持（cffi 被 aiohttp/部分库间接需要）----
cffi==2.0.0
pycparser==3.0
